# Android App
archive/SmartBMS_App/

# Claude Code data
.claude/
status*.json
find_pi.py

# Python virtual environment (recreate from CANbus_data/requirements.txt)
venv/

# Python bytecode caches
__pycache__/
*.pyc

# Playwright MCP browser debug artifacts
.playwright-mcp/

# CAN capture / probe logs (curated test captures live in CANbus_data/tests/fixtures/)
logs/
*.log

# Secrets — never commit tokens or env files
HA_TOKEN.txt
.env
prompt-history*.md

# Webcam capture scratch (e-paper display verification)
.webcam/

# MS Office owner/lock files (created while a doc is open)
~$*

# large HAOS data for recovery
haos_recovery_data/

# Golden render images — regenerated on demand by display/tests/golden_harness.py.
# Only the hashes.txt baseline (and the committed DejaVu fonts) are tracked.
display/tests/golden/*.png

logs/