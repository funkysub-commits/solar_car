# Python dependencies for the CANbus_data tools on the Raspberry Pi.
#
# Recreate the environment on the Pi:
#   python3 -m venv ~/canbus-venv
#   ~/canbus-venv/bin/pip install -r requirements-pi.txt
#
# The SH-C31G runs slcan firmware and appears as a serial port, so python-can
# talks to it via its slcan backend (pyserial). No USB/libusb packages needed.

python-can==4.6.1
pyserial==3.5
